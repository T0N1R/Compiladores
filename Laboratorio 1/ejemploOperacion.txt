class A {
   car: Int;
};

class Main inherits IO {

   var : Int <- 0;

   value() : Int { var };

   set_var(num : Int) : SELF_TYPE {
      {
         var <- num;
         self;
      }
   };

   init(i : Int, rest : List) : List {
      {
	 -- car <- pi;
	 -- cdr <- rest;
    var <- "holla"
	 self;
      }
   };

   main(): SELF_TYPE {
	out_string("Hello, World.\n")
   };
};
