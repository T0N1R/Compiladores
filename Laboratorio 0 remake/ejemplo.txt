class Silly {
copy() : SELF_TYPE { self };
};

class Main inherits IO {
  
  a : Int;
  b : Int;
  c : Int;
  a <- a + b * c ;
  self;

  C<=P;

  init(hd : Int, tl : List) : Cons {
    xcar <- hd;
    xcdr <- tl;
    self;
  }

  c.init(1,L)

  (new Cons).init(1, new List)

  (isvoid self).type_name()


};
